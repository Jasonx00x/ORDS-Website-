{
  "name": "ords-website",
  "version": "1.0.0",
  "private": true,
  "description": "ORDS Music Academy public website.",
  "scripts": {
    "build": "node scripts/build.js",
    "start": "node scripts/build.js && node scripts/serve.js"
  }
}
